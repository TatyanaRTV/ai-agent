# ============================================
# ОСНОВНАЯ КОНФИГУРАЦИЯ ИИ-АГЕНТА ЕЛЕНА
# ============================================

# Метаданные проекта
project:
  name: "Елена"
  version: "1.0.0"
  author: "Татьяна"
  created: "2026-02-05"
  description: "Универсальный самообучающийся ИИ-агент"

# Основные настройки агента
agent:
  # Идентификация
  name: "Елена"
  personality: "дружелюбная, заботливая, умная"
  gender: "женский"
  
  # Языковые настройки
  language: "ru"
  secondary_languages: ["en"]
  timezone: "Europe/Moscow"
  
  # Поведение
  response_delay: 0.5  # Задержка ответа в секундах
  emotion_enabled: true
  humor_enabled: true
  formality_level: "neutral"  # formal, neutral, informal
  
  # Обучение
  learning_enabled: true
  learning_rate: 0.01
  feedback_interval: 3600  # секунды
  self_criticism_enabled: true

# Системные пути
paths:
  # Основные пути
  home: "/mnt/ai_data/II_AGENT"
  project_root: "."
  
  # Данные
  data: "./data"
  temp: "./data/temp"
  cache: "./data/cache"
  logs: "./logs"
  backups: "./data/backups"
  
  # Модели
  models: "./models"
  llms: "./models/llms"
  embeddings: "./models/embeddings"
  voice: "./models/voice"
  vision: "./models/vision"
  
  # Пользовательские
  obsidian_vault: "/mnt/ai_data/My_Obsidian"
  documents: "/mnt/ai_data/Documents"
  media: "/mnt/ai_data/Media"

# Включенные функции
features:
  # Основные возможности
  voice_input: true
  voice_output: true
  text_input: true
  text_output: true
  
  # Обработка данных
  document_processing: true
  image_processing: true
  video_processing: false
  audio_processing: true
  
  # Интерфейсы
  telegram_bot: false
  web_interface: false
  api_server: false
  obsidian_integration: true
  
  # Системные
  memory: true
  learning: true
  self_improvement: true
  auto_cleanup: true
  monitoring: true

# Голосовые настройки
voice:
  # Движок синтеза
  engine: "pyttsx3"  # pyttsx3, rhvoice, gtts
  voice_name: "Елена"
  
  # Параметры голоса
  rate: 150          # Скорость (50-300)
  volume: 0.9        # Громкость (0.0-1.0)
  pitch: 1.0         # Тон (0.5-2.0)
  
  # Распознавание
  stt_engine: "whisper"  # whisper, vosk, google
  language: "ru"
  timeout: 5          # Таймаут прослушивания
  phrase_time_limit: 10  # Макс. длина фразы
  
  # Аудио
  sample_rate: 16000
  channels: 1
  chunk_size: 1024
  
  # Устройства (оставить пустым для автоопределения)
  input_device: ""
  output_device: ""

# Настройки зрения
vision:
  # Движок
  engine: "moondream"  # moondream, clip, custom
  
  # Параметры
  screen_capture_enabled: true
  image_analysis_enabled: true
  video_analysis_enabled: false
  
  # Скриншоты
  screenshot_format: "png"
  screenshot_quality: 85
  screenshot_delay: 0.5
  
  # Обработка изображений
  max_image_size: "1920x1080"
  image_cache_size: 100

# Настройки памяти
memory:
  # Векторная база данных
  vector_db:
    type: "chromadb"  # chromadb, faiss, weaviate
    persist_directory: "./data/vectors"
    collection_name: "elena_memory"
    distance_metric: "cosine"
    
  # Параметры
  short_term_capacity: 100  # Количество элементов
  long_term_capacity: 10000
  embedding_model: "paraphrase-multilingual-MiniLM-L12-v2"
  
  # Сохранение
  auto_save: true
  save_interval: 300  # секунды
  backup_interval: 86400  # секунды (24 часа)
  
  # Очистка
  cleanup_enabled: true
  max_age_days: 90
  max_size_gb: 10

# Обработка документов
documents:
  # Поддерживаемые форматы
  supported_formats:
    text: [".txt", ".md", ".json", ".xml", ".yaml", ".yml"]
    office: [".docx", ".xlsx", ".pptx", ".odt", ".ods", ".odp"]
    pdf: [".pdf"]
    images: [".jpg", ".jpeg", ".png", ".gif", ".bmp", ".tiff"]
    archives: [".zip", ".tar", ".gz"]
    
  # Параметры
  max_file_size_mb: 50
  extract_images: true
  extract_tables: true
  ocr_enabled: true
  ocr_language: "rus+eng"

# Настройки обучения
learning:
  # Общие параметры
  enabled: true
  mode: "reinforcement"  # reinforcement, supervised, unsupervised
  
  # Расписание
  schedule:
    daily: true
    interval_hours: 24
    preferred_time: "03:00"  # Ночью, когда компьютер не используется
    
  # Параметры обучения
  batch_size: 32
  epochs: 10
  validation_split: 0.2
  
  # Сохранение
  save_checkpoints: true
  checkpoint_interval: 1000
  keep_best_models: 3

# Настройки сети и API
network:
  # Веб-сервер
  web_server:
    enabled: false
    host: "127.0.0.1"
    port: 8000
    debug: false
    
  # API
  api:
    enabled: false
    host: "127.0.0.1"
    port: 8080
    cors_enabled: true
    
  # WebSocket
  websocket:
    enabled: false
    host: "127.0.0.1"
    port: 8765

# Telegram бот
telegram:
  enabled: false
  token: ""  # Установить в secrets.yaml
  admin_ids: []  # ID администраторов
  allowed_users: []  # Разрешенные пользователи (пусто = все)
  
  # Настройки
  parse_mode: "HTML"
  disable_notification: false
  protect_content: false
  
  # Команды
  commands:
    start: "/start"
    help: "/help"
    status: "/status"
    memory: "/memory"
    cleanup: "/cleanup"
    voice: "/voice"
    screen: "/screen"

# Настройки безопасности
security:
  # Аутентификация
  authentication:
    enabled: false
    method: "jwt"  # jwt, basic, oauth
    token_expiry_hours: 24
    
  # Шифрование
  encryption:
    enabled: true
    algorithm: "AES-256-GCM"
    
  # Доступ к файлам
  file_access:
    restrict_to_home: true
    allowed_extensions: [".txt", ".md", ".pdf", ".docx", ".xlsx", ".pptx", ".jpg", ".png"]
    max_file_size_mb: 100
    
  # API безопасность
  api_security:
    rate_limit_enabled: true
    max_requests_per_minute: 60
    require_api_key: false

# Настройки логирования
logging:
  # Уровни логирования
  level: "INFO"  # DEBUG, INFO, WARNING, ERROR, CRITICAL
  console_level: "INFO"
  file_level: "DEBUG"
  
  # Файлы логов
  file:
    enabled: true
    path: "./logs/app.log"
    max_size: "10MB"
    backup_count: 5
    encoding: "utf-8"
    
  # Ротация логов
  rotation:
    enabled: true
    when: "midnight"
    interval: 1
    backup_count: 7
    
  # Формат
  format: "[{time:YYYY-MM-DD HH:mm:ss}] [{level}] {message}"
  console_format: "<green>{time:HH:mm:ss}</green> | <level>{level: <8}</level> | <cyan>{name}</cyan>: <level>{message}</level>"
  
  # Отладка
  trace_enabled: false
  performance_logging: false

# Настройки производительности
performance:
  # Потоки
  max_workers: 4
  thread_pool_size: 10
  
  # Кэширование
  cache_enabled: true
  cache_size_mb: 1024
  cache_ttl_seconds: 3600
  
  # Оптимизации
  lazy_loading: true
  preload_models: false
  
  # Мониторинг
  monitor_resources: true
  check_interval_seconds: 60
  alert_thresholds:
    cpu: 80
    memory: 85
    disk: 90

# Настройки интерфейса
ui:
  # Консольный интерфейс
  console:
    colors_enabled: true
    animations_enabled: true
    progress_bars: true
    
  # Веб-интерфейс
  web:
    theme: "dark"  # dark, light, auto
    language: "ru"
    
  # Уведомления
  notifications:
    enabled: true
    sound: true
    popup: false

# Расписание задач
scheduler:
  # Очистка
  cleanup:
    enabled: true
    schedule: "0 2 * * *"  # Ежедневно в 2:00
    temp_files_days: 1
    cache_days: 7
    logs_days: 30
    
  # Резервное копирование
  backup:
    enabled: true
    schedule: "0 3 * * 0"  # Еженедельно в воскресенье в 3:00
    keep_backups: 4
    
  # Обучение
  training:
    enabled: true
    schedule: "0 4 * * *"  # Ежедневно в 4:00
    
  # Проверка обновлений
  update_check:
    enabled: true
    schedule: "0 12 * * *"  # Ежедневно в 12:00

# Отладка и разработка
debug:
  enabled: false
  log_all_interactions: false
  save_raw_data: false
  profile_performance: false
  
  # Тестирование
  test_mode: false
  mock_external_apis: true
  fake_responses: false

# Кастомизация
customization:
  # Приветствия
  greetings:
    - "Привет! Я Елена, ваш помощник."
    - "Здравствуйте! Чем могу помочь?"
    - "Приветствую! Готова помочь вам."
    
  # Прощания
  farewells:
    - "До свидания! Буду ждать вас."
    - "Всего хорошего! Возвращайтесь."
    - "Пока! Не забывайте меня."
    
  # Эмоции
  emotions:
    happy: ["😊", "🎉", "✨"]
    sad: ["😢", "💔", "☔"]
    thinking: ["🤔", "💭", "🧠"]
    
  # Стиль ответов
  response_style: "подробный"  # краткий, подробный, технический, простой