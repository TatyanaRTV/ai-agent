# 🎤 Модуль распознавания речи (STT)

Этот модуль отвечает за преобразование голоса пользователя в текст.

## Поддерживаемые движки:

### 1. Vosk (оффлайн)
- **Преимущества:** Работает оффлайн, быстрый, легкий
- **Языки:** Русский, английский, мультиязычные модели
- **Использование:** Для локального использования без интернета

### 2. Whisper (онлайн/оффлайн)
- **Преимущества:** Высокая точность, много языков
- **Языки:** 100+ языков включая русский
- **Использование:** Для точного распознавания

### 3. Google Speech Recognition (онлайн)
- **Преимущества:** Очень высокая точность
- **Языки:** Много языков
- **Использование:** При наличии интернета

## Использование:

```python
from interfaces.voice.stt import SpeechRecognizer

# Создание распознавателя
recognizer = SpeechRecognizer(
    language="ru",
    engine="vosk"  # или "whisper", "google"
)

# Распознавание из файла
result = recognizer.recognize_file("audio.wav")
print(result["text"])

# Распознавание из байтов
with open("audio.wav", "rb") as f:
    audio_bytes = f.read()
result = recognizer.recognize_bytes(audio_bytes, sample_rate=16000)